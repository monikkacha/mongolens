Hook up Home page to backend

Design “Connections Dashboard”

Build “Database → Collections → Documents” explorer

Monaco-based query console

Saved query templates UI

Global layout (sidebar + topbar)
